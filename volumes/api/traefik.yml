api:
  dashboard: true
  insecure: true

entryPoints:
  web:
    address: ":8000"
  websecure:
    address: ":8443"

providers:
  file:
    filename: "/etc/traefik/dynamic.yml"
    watch: true

log:
  level: INFO
